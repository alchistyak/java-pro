server:
  servlet:
    context-path: "/payments"
  port: 8282

integrations:
  executors:
    executor-payments-properties:
      url: "http://localhost:8080/products"
      connect-timeout: 1s
      read-timeout: 30s
